{
  "name": "modern-shop-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -c magenta,cyan -n server,client \"yarn workspace @shop/server dev\" \"yarn workspace @shop/client dev\"",
    "build": "yarn workspace @shop/server build && yarn workspace @shop/client build",
    "start": "yarn workspace @shop/server start",
    "format": "echo 'add a formatter if you like'"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "mongodb": "^2.2.12"
  }
}
