{
  "name": "modern-shop-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -c magenta,cyan -n server,client \"npm run -w @shop/server dev\" \"npm run -w @shop/client dev\"",
    "build": "yarn install --production=false && yarn workspace @shop/server build && yarn workspace @shop/client build",
    "start": "npm run -w @shop/server start",
    "dev:server": "npm run -w @shop/server dev",
    "dev:client": "npm run -w @shop/client dev",
    "format": "echo 'add a formatter if you like'"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "mongodb": "^2.2.12"
  }
}
